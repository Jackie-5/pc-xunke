/**
 * Created by JackieWu on 2019/6/14
 */

// const getErshoufangList = require('../../public-api/getErshoufangList');
// const getLoupanList = require('../../public-api/getLoupanList');
// const getXiaoquList = require('../../public-api/getXiaoquList');

module.exports = {

  // ershoufangList: async (ctx) => {
  //   const { currentCity, basisCurrentDomain } = ctx.state;
  //
  //   return await getErshoufangList(ctx, {
  //     start: 0,
  //     rows: 10,
  //     query: `*:* AND cityCode:${currentCity.id} AND domain:${basisCurrentDomain.currentDomain}`,
  //   });
  //
  // },
  // loupanList: async (ctx) => {
  //   const { currentCity, basisCurrentDomain } = ctx.state;
  //   return await getLoupanList(ctx, {
  //     start: 0,
  //     rows: 10,
  //     query: `*:* AND cityCode:${currentCity.id} AND domain:${basisCurrentDomain.currentDomain}`,
  //   });
  // },
  //
  // xiaoquList: async (ctx) => {
  //   const { currentCity, basisCurrentDomain } = ctx.state;
  //
  //   return await getXiaoquList(ctx, {
  //     start: 0,
  //     rows: 10,
  //     query: `*:* AND cityCode:${currentCity.id} AND domain:${basisCurrentDomain.currentDomain}`,
  //   });
  // },
};
